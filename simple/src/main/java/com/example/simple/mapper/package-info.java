/**
 * Mapper interfaces processed by {@code kyra-orm-processor}.
 * <p>
 * {@link com.example.simple.mapper.UserMapper} — full CRUD/query DSL with XML mappings.
 * {@link com.example.simple.mapper.MixedUserMapper} — mixed capability traits with per-trait generics.
 * {@link com.example.simple.mapper.PairUserMapper} — record result mapping with generic element types.
 * {@link com.example.simple.mapper.PlainUserMapper} — minimal read-only mapper.
 */
package com.example.simple.mapper;
