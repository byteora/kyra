<mapper namespace="org.byteora.kyra.quarkus.deployment.test.mapper.TestUserMapper">
    <select id="selectById">
        select id, name
        from test_user
        where id = #{id}
    </select>
</mapper>
